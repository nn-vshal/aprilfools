[build]
  publish = "site"
  functions = "api/functions"
  command = "npm run build"

[dev]
  publish = "site"
  functions = "api/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
